export const TYPES=['Called Out','PTO','Sick Leave','Vacation','Other'];
export function clean(v,n=500){return String(v??'').trim().slice(0,n)}
export function validDate(v){return /^\d{4}-\d{2}-\d{2}$/.test(v)&&!Number.isNaN(Date.parse(v+'T00:00:00Z'))}
export function earliestAdvanceDate(){const d=new Date();d.setUTCHours(0,0,0,0);d.setUTCDate(d.getUTCDate()+2);return d.toISOString().slice(0,10)}
export function validateEntry(x,{enforceAdvance=true}={}){const submitter_type=clean(x.submitter_type,20),name=clean(x.name,120),request_type=clean(x.request_type,40),event_date=clean(x.event_date,10),reason=clean(x.reason,1000);if(!['Employee','Parent'].includes(submitter_type))return'Choose employee or parent.';if(!name)return'Enter the employee or client name.';if(!TYPES.includes(request_type))return'Choose a valid request type.';if(!validDate(event_date))return'Choose a valid date.';if(enforceAdvance&&submitter_type==='Employee'&&['PTO','Sick Leave'].includes(request_type)&&event_date<earliestAdvanceDate())return'Employees must submit PTO and Sick Leave at least 48 hours in advance.';if(['Called Out','Other'].includes(request_type)&&!reason)return'Additional details are required for this request type.';return{submitter_type,name,request_type,event_date,reason};}
