'use client';import{useState}from'react';import Brand from './components/Brand';
export default function Home(){const[f,setF]=useState({submitter_type:'',name:'',request_type:'Called Out',event_date:'',reason:''}),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const details=['Called Out','Sick Leave','Other'].includes(f.request_type),cutoff=advanceDate(),ptoRestricted=f.submitter_type==='Employee'&&(!f.event_date||f.event_date<cutoff);function update(patch){const next={...f,...patch};if(patch.submitter_type==='Parent')next.request_type='Called Out';if(next.submitter_type==='Employee'&&next.event_date&&next.event_date<cutoff&&next.request_type==='PTO')next.request_type='Called Out';setF(next)}async function submit(e){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/callouts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)}),j=await r.json();setBusy(false);if(!r.ok)return setMsg(j.error);setMsg('Submission completed successfully.');setF({...f,name:'',event_date:'',reason:''})}const requestOptions=f.submitter_type==='Parent'?['Called Out']:['Called Out','PTO','Sick Leave','Other'];return <><header><Brand subtitle="Parsippany Call-Out & Time-Off Portal"/><nav className="portal-links"><a className="link" href="/calendar">Team calendar</a><a className="link" href="/manager">Manager calendar</a></nav></header><section className="hero"><div><label>SUCCESS ON THE SPECTRUM · PARSIPPANY</label><h1>Report an absence or planned time off</h1><p>Notify the Parsippany management team when an employee or client will be unavailable.</p></div></section><main><form className="card form" onSubmit={submit}><Field label="I am submitting this as"><select required value={f.submitter_type} onChange={e=>update({submitter_type:e.target.value})}><option value="">Select employee or parent</option><option>Employee</option><option>Parent</option></select></Field><Field label="Employee or client name"><input required placeholder="Enter the full name" value={f.name} onChange={e=>update({name:e.target.value})}/></Field><Field label="Request type"><select value={f.request_type} onChange={e=>update({request_type:e.target.value})}>{requestOptions.map(option=><option key={option} disabled={option==='PTO'&&ptoRestricted}>{option}</option>)}</select>{f.submitter_type==='Employee'&&<small className="muted">PTO must be submitted at least 48 hours in advance. Sick Leave requires a reason.</small>}{f.submitter_type==='Parent'&&<small className="muted">Parent submissions are recorded as Called Out.</small>}</Field><Field label="Date of absence"><input required type="date" value={f.event_date} onChange={e=>update({event_date:e.target.value})}/></Field><div className="wide"><Field label={`Reason${details?' (required)':' (optional)'}`}><textarea required={details} placeholder="Briefly explain the absence." value={f.reason} onChange={e=>update({reason:e.target.value})}/></Field><p className="muted">Submission details and submission time are available only to authorized managers.</p>{msg&&<p className="message">{msg}</p>}<button disabled={busy}>{busy?'Submitting…':'Submit'}</button></div></form></main></>}
function advanceDate(){const d=new Date();d.setHours(0,0,0,0);d.setDate(d.getDate()+2);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function Field({label,children}){return <label className="field"><b>{label}</b>{children}</label>}
