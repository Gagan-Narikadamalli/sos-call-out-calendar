'use client';import{useState}from'react';
export default function Home(){const[f,setF]=useState({submitter_type:'',name:'',request_type:'Called Out',event_date:'',reason:''}),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const details=['Called Out','Other'].includes(f.request_type),cutoff=advanceDate(),restricted=f.submitter_type==='Employee'&&(!f.event_date||f.event_date<cutoff);function update(patch){const next={...f,...patch};if(next.submitter_type==='Employee'&&next.event_date&&next.event_date<cutoff&&['PTO','Sick Leave'].includes(next.request_type))next.request_type='Called Out';setF(next)}async function submit(e){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/callouts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)}),j=await r.json();setBusy(false);if(!r.ok)return setMsg(j.error);setMsg('Submission completed successfully.');setF({...f,name:'',event_date:'',reason:''})}return <><header><div className="brand"><b>SOS</b><span><strong>Success On The Spectrum</strong><small>Call-Out &amp; Time-Off Portal</small></span></div></header><section className="hero"><div><label>CALL-OUT &amp; TIME-OFF REQUEST</label><h1>Report an absence or planned time off</h1><p>Use this form to notify the Success On The Spectrum management team when an employee or client will be unavailable.</p></div></section><main><form className="card form" onSubmit={submit}><Field label="I am submitting this as"><select required value={f.submitter_type} onChange={e=>update({submitter_type:e.target.value})}><option value="">Select employee or parent</option><option>Employee</option><option>Parent</option></select></Field><Field label="Employee or client name"><input required placeholder="Enter the full name" value={f.name} onChange={e=>update({name:e.target.value})}/></Field><Field label="Request type"><select value={f.request_type} onChange={e=>update({request_type:e.target.value})}><option>Called Out</option><option disabled={restricted}>PTO</option><option disabled={restricted}>Sick Leave</option><option>Vacation</option><option>Other</option></select>{f.submitter_type==='Employee'&&<small className="muted">PTO and Sick Leave must be submitted at least 48 hours in advance.</small>}</Field><Field label="Date of absence"><input required type="date" value={f.event_date} onChange={e=>update({event_date:e.target.value})}/></Field><div className="wide"><Field label={`Additional details${details?' (required)':' (optional)'}`}><textarea required={details} placeholder="Briefly explain the absence." value={f.reason} onChange={e=>update({reason:e.target.value})}/></Field><p className="muted">Your submission details and submission time are available only to authorized managers.</p>{msg&&<p className="message">{msg}</p>}<button disabled={busy}>{busy?'Submitting…':'Submit'}</button></div></form></main></>}
function advanceDate(){const d=new Date();d.setHours(0,0,0,0);d.setDate(d.getDate()+2);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function Field({label,children}){return <label className="field"><b>{label}</b>{children}</label>}
